/*
 * Arquivo: GlobalStyle.js
 * Objetivo: Definir estilos CSS globais para toda a aplicação.
 * Biblioteca: styled-components (que instalámos na Tarefa [0071])
 */

// Importa a função 'createGlobalStyle' da biblioteca 'styled-components'
import { createGlobalStyle } from 'styled-components';

/**
 * GlobalStyle
 *
 * O que faz?
 * Este é um componente especial do 'styled-components' que, quando
 * usado na nossa aplicação, injeta estes estilos CSS na página inteira.
 *
 * Porque usamos?
 * 1. Reset CSS: Removemos estilos padrão (margens, paddings) que
 * os navegadores aplicam, para termos controlo total.
 * 2. Padrões: Definimos a fonte (font-family), a cor de fundo
 * (background-color) e a cor do texto (color) de todo o site
 * num só local.
 */
const GlobalStyle = createGlobalStyle`
  /* 1. Reset CSS */
  *, *::before, *::after {
    box-sizing: border-box; /* Garante que 'padding' e 'border' não aumentem o tamanho final do elemento */
    margin: 0;
    padding: 0;
  }

  /* 2. Padrões Globais */
  body {
    /* Define uma fonte moderna e limpa, padrão em muitos sistemas */
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    
    /* Cor de fundo cinza claro, comum em dashboards */
    background-color: #f0f2f5; 
    
    /* Cor de texto padrão (cinza escuro, mais suave que preto puro) */
    color: #333; 
    
    line-height: 1.6; /* Melhora a legibilidade do texto */
  }

  /* 3. Padrões de Elementos (Exemplos) */
  
  button {
    cursor: pointer;
    font-family: inherit; /* Garante que botões usem a mesma fonte do 'body' */
    padding: 8px 12px;
    border: 1px solid transparent;
    border-radius: 4px;
    transition: background-color 0.2s; /* Efeito suave ao passar o rato */
  }

  input, select, textarea {
    font-family: inherit; /* Garante que inputs usem a mesma fonte */
    padding: 8px;
    border: 1px solid #ccc;
    border-radius: 4px;
    width: 100%; /* Faz com que ocupem o espaço disponível */
    box-sizing: border-box; /* Importante para o 'width: 100%' funcionar bem com 'padding' */
  }
`;

// Exportamos o componente para que o App.jsx possa importá-lo e usá-lo
export default GlobalStyle;